import Foundation

/// Namespace that exposes the `MyToyboxMedia` SPM module's resource bundle.
public enum MyToyboxMedia {
    /// The bundle containing media assets (images, videos) bundled with this module.
    public static let bundle: Bundle = .module
}
